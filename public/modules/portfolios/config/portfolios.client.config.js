/*'use strict';

// Configuring the Articles module
angular.module('portfolios').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		Menus.addMenuItem('topbar', 'Portfolios', 'portfolios', 'dropdown', '/portfolios(/create)?');
		Menus.addSubMenuItem('topbar', 'portfolios', 'List Portfolios', 'portfolios');
		Menus.addSubMenuItem('topbar', 'portfolios', 'New Portfolio', 'portfolios/create');
	}
]);*/